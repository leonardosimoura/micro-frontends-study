{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "prestart": "lerna bootstrap",
    "start": "lerna run --scope @application/* --parallel start",
    "build": "lerna run --scope @application/* build",
    "serve": "lerna run --scope @application/* --parallel serve",
    "clean": "lerna run --scope @application/* --parallel clean"
  },
  "dependencies": {}
}